# .env.example

# URL اتصال به دیتابیس PostgreSQL
# فرمت: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/user_manager_db?schema=public"

# ⚠️ هنگام استفاده:
# 1. این فایل را به `.env` کپی کنید:
#    cp .env.example .env
# 2. مقادیر را با اطلاعات واقعی دیتابیس خود جایگزین کنید.